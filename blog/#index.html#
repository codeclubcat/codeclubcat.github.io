---
layout: default
title: Som el CodeClub de Catalunya, Hola! :)
---

<div class="row">
  <div class="eight columns">
    <!-- This loops through the paginated posts -->
    {% for post in site.posts reversed %}
    <h1><a href="{{ post.url }}">{{ post.title }}</a></h1>
    Escrit per:  <b><span class="author">{{ post.author }}</span></b> 
    &nbsp;&nbsp;&nbsp;  <span class="date"> {{ post.date | date: "%d/%m/%Y" }} </span>
    &nbsp;&nbsp;&nbsp;  Categories: <span class="tags"> {{ post.tags }} </span>

    <div class="content">
      <br/>
      <br/>
      {{ post.content }}
    </div>
    <br />
    <hr />
    {% endfor %}
  </div>
    
    <!-- Pagination links
    <div class="pagination">
      {% if paginator.previous_page %}
      <a href="/page{{ paginator.previous_page }}" class="previous">Previous</a>
      {% else %}
      <span class="previous">Previous</span>
      {% endif %}
      <span class="page_number ">Page: {{ paginator.page }} of {{ paginator.total_pages }}</span>
      {% if paginator.next_page %}
      <a href="/page{{ paginator.next_page }}" class="next">Next</a>
      {% else %}
      <span class="next ">Next</span>
      {% endif %}
    </div>
    -->

    
  <!-- On the right, we show all published posts! -->
  <div class="four columns">
    <h2>Últimes entrades</h2>
    {% for post in site.posts reversed %}
    <h6><a href="{{ post.url }}">{{ post.title }}</a></h6>
    {% endfor %}

    
    <h2>Tags</h2>

    <!-- create a list of categories -->
    {% for tag in site.tags %}
    <a href="/blog/categories/{{ tag[0] }}.html"> {{ tag[0] }}</a>
    {% endfor %}

    <!-- ? -->
    {% for tag in site.tags %}
    
    
    <div class="postmeta extract">
      <p class="timestamp">
	{{ post.date }}
	<br/>
	<span class="time"></span>
    </div>

    <div class="post extract">
      
      <h3><a href="{{ post.url }}">{{ post.title }}</a></h3>


      <p>{{ post.content | strip_html | truncatewords:80 }}</p>

      <div class="blocked tags">
	<p>
	  {% for tag in post.categories   %}
	  <a href="/categories/{{ tag}}.html"></a>
	  ,
	  {% endfor %}
	</p>
      </div>
      <div class="hr"></div>
    </div>

    <div class="c">&nbsp;</div>

   {% endfor %}
    
</div> 
